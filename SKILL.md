---
name: token-discipline
description: 在多步文件编辑、编程或工具密集任务中减少重复读取、冗长输出、无效轮询与返工。用户要求节省token或优化执行开销时使用；不为普通简答增加流程。
---

# Token 使用自律

减少无效步骤与上下文，完整交付和必要验证优先。遵守明确预算，但不编造token用量、节省比例或预算耗尽后的完成状态。

## 执行规则

1. **明确终点。** 内部确定交付范围、关键风险和验收条件。选择主工具链，复用本任务已确认的环境与素材；适用规范读一次，按需读取引用。
2. **区分浏览与处理。** 先搜索，再读相关段落、页码或字段。只限制回传给模型的浏览内容；计算、转换与编辑所需的底层数据必须完整，不能把截断结果当作完整输入。
3. **合并独立探测。** 批量检查必要路径、命令和依赖；逐项捕获结果，避免首个缺包掩盖其余情况。依赖前一步结果的操作顺序执行。
4. **控制输出。** 返回关键事实、错误与少量上下文；完整日志落盘并保留路径。出现截断，先缩小查询。图片走图像通道，二进制与base64不打印为长文本。
5. **重试需有依据。** 同样失败且条件未变，不盲目重复。批处理中出现限流、鉴权或环境故障等系统性错误，停止同类请求，诊断后换路或等待。成功路径在本任务内复用，不能绕过权限或把临时故障写成永久规则。
6. **等待不等于进展。** 优先完成事件或工具返回的等待机制；必须轮询时按耗时预期退避，通常间隔30–60秒。遵守平台时限和沟通要求，不重复叙述未变化的状态。
7. **先验证再复制。** 文档与PPT先验证代表性复杂页面的字体、文字框和布局，再批量应用。修改只重验受影响部分；共享模板变化才扩大检查。具体规则见[文件工作流](references/file-workflows.md)，仅此类任务读取。
8. **保留有效上下文。** 长任务需要交接时，仅保留目标、已确认事实、文件位置、未决问题与下一步。不反复生成账本，不声称能修改宿主历史、缓存或系统指令。
9. **谨慎委派。** 只有明确授权或适用指令允许、且子任务独立有价值时使用代理；传必要文件、约束与简短任务，避免默认复制完整历史。不擅自更换模型或限制必要测试数量。
10. **通过即交付。** 完成全部范围并通过必要检查后保存交付。失败或缺失的检查不能报通过；不为省token跳过关键验证，也不新增未请求的版本、装饰或审查。

每次调用前，内部确认它解决哪个未知、是否已有答案、能否缩小范围。重复读取、连续失败或第二轮返工是诊断信号，不是追加更多调用的理由。

## 评估与来源

只有用户要求衡量效果或需要验证技能改动时，读[评估方法](references/evaluation.md)。参考项目及取舍见[研究记录](references/research.md)，正常任务不加载。技能不能保证每次节省，也不能替代平台的真实用量计量。